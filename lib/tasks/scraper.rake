namespace :scrape do

  desc "scrape today's Covid update for Nashville"
  task get_latest_update: :environment do
    # puts "Updating @blockwork_cc's twitter feed..."
    # TwitterBot.fire_tweet
    # puts "tweet sent!"
  end

end
